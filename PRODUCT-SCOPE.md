# A股看盘工作台范围

## 产品目标

本网站只辅助个人投资者盘后看市场、找方向、列观察对象和记录次日计划。它不替用户下单，不接 AI 模型接口，不承诺预测收益。

## 每日核心流程

1. 市场全景：确认市场状态、赚钱效应和风险。
2. 板块轮动：确认主线、备选方向和退潮方向。
3. 个股透视：查看 K 线、量价、资金代理、支撑压力和相对强弱。
4. 我的关注：保留真正需要跟踪的板块和股票。
5. 市场复盘：手写结论、触发条件、放弃条件和次日计划。

## 保留功能

- 市场状态、涨跌宽度、成交与情绪证据。
- 板块资金、强弱、持续性和轮动观察。
- 消息影响对象及后续价格验证。
- 个股 K 线、成交量、相对板块强弱、支撑和压力。
- 浏览器本地关注列表与市场复盘。
- 数据时间、来源、缺失项和置信度说明。
- 量价观察池：异常上冲、整理观察、转强确认和失效记录；关联已有个股详情。探索规则尚未完成独立样本回测，不输出控盘概率。

## 暂不增加

- AI API 或自动生成交易建议。
- 自动下单和券商账户连接。
- 盘中高频提醒。
- 无法验证的真实主力身份判断。
- 复杂机构级组合优化。
- 为了展示而增加、但不能改变看盘决策的指标。

## 数据边界

- GitHub Pages 是只读静态看板，定时任务更新公开市场数据。
- 收盘价格优先使用授权接口，失败时使用 AkShare 的东方财富/新浪全市场快照；盘中数据不会写成已完成日线。
- 免费资金流依次尝试东方财富排名和同花顺资金流，并与同批价格校验；覆盖或价格一致性不足时拒绝落盘。
- 价格、估值、主力资金和涨跌停池分别记录截止日期，不要求为了构建而伪造为同一天。
- 主力资金只有净额、缺少大小单明细时，不生成机构、游资或散户代理曲线。
- 当资金日期比价格落后三个自然日以上时，资金结论降为低置信度，模型仓位上限压到 20%，并要求恢复同日资金后重新计算。
- 关注列表和复盘保存在当前浏览器的 `localStorage`，不会上传到 GitHub。
- 更换浏览器、清理网站数据或更换设备不会自动同步，重要复盘应导出 Markdown 留档。
- 数据过期或缺失时必须显示状态，不能把旧数据包装成当日结论。
- 行情服务认证失败时明确报错，不将其视为无成交。交易日历缺失时只允许用实际指数行情日期兜底，不猜测交易日；不完整日线不得覆盖已有快照。
- 网站可保留可用旧快照，但数据新鲜度校验失败时更新任务最终必须标记失败。可通过工作流的 diagnose_only 输入只读检查数据源，不发布页面。
